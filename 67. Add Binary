/*
67. Add Binary
Given two binary strings a and b, return their sum as a binary string.

Example 1:
Input: a = "11", b = "1"
Output: "100"

Example 2:
Input: a = "1010", b = "1011"
Output: "10101"
*/

class Solution(object):
    def addBinary(self, a, b):
        """
        :type a: str
        :type b: str
        :rtype: str
        """
        # 1. Convert binary strings 'a' and 'b' to decimal integers.
        #    The int(string, 2) function interprets the string as a base-2 number.
        bin_a = int(a, 2)
        bin_b = int(b, 2)
        
        # 2. Add the two decimal integers together.
        val = bin_a + bin_b
        
        # 3. Convert the resulting sum back into a binary string.
        #    The bin() function returns a string starting with '0b'.
        bin_val = bin(val)
        
        # 4. Return the binary string excluding the '0b' prefix.
        return bin_val[2:]
