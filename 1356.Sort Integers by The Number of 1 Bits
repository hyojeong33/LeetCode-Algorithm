/*
You are given an integer array arr. 
Sort the integers in the array in ascending order by the number of 1's in their binary representation and 
in case of two or more integers have the same number of 1's you have to sort them in ascending order.
Return the array after sorting it.

Example 1:
Input: arr = [0,1,2,3,4,5,6,7,8]
Output: [0,1,2,4,8,3,5,6,7]
Explantion: [0] is the only integer with 0 bits.
[1,2,4,8] all have 1 bit.
[3,5,6] have 2 bits.
[7] has 3 bits.
The sorted array by bits is [0,1,2,4,8,3,5,6,7]

Example 2:
Input: arr = [1024,512,256,128,64,32,16,8,4,2,1]
Output: [1,2,4,8,16,32,64,128,256,512,1024]
Explantion: All integers have 1 bit in the binary representation, you should just sort them in ascending order.
 
Constraints:
1 <= arr.length <= 500
0 <= arr[i] <= 104
*/

class Solution(object):
    def sortByBits(self, arr):
        """
        :type arr: List[int]
        :rtype: List[int]
        """
        arr.sort(key=lambda x: (bin(x).count('1'), x))
        return arr

---------------------------------------------------------
1. 비트(Bit)란?
* 정의: 컴퓨터가 정보를 처리하는 최소 단위 (0 또는 1).
* 십진수를 이진수로 변환: 숫자를 2로 계속 나누어 나머지를 기록함.
    * 예: $7 \rightarrow 111$ (1이 3개), $8 \rightarrow 1000$ (1이 1개)
2. 파이썬에서 1의 개수 세기 (bin().count())
* bin(x): 숫자 x를 이진수 문자열로 변환 (예: 7 → '0b111').
* .count('1'): 문자열 내에서 특정 문자('1')의 개수를 반환.
* 결과: bin(7).count('1')은 정수 3을 반환함.

3. 파이썬 sort()의 key 매개변수
* 개념: "원래 데이터는 건드리지 않고, **특정한 기준(Key)**에 의해서만 순서를 정해라"라는 명령어.
* 작동 원리:
    1. 리스트의 각 요소에 임시 '이름표(Key)'를 붙임.
    2. 이름표의 값을 비교해서 줄을 세움.
    3. 줄 세우기가 끝나면 이름표를 떼고 원래 데이터를 그 자리에 둠.

4. 람다(lambda)와 튜플(tuple) 정렬
* lambda x: 정렬 기준을 만들기 위한 일회용 함수.
* 튜플 정렬 규칙: (A, B) 형태의 기준을 주면 파이썬은 다음 순서로 비교함.
    * 1순위: A를 비교해서 정렬.
    * 2순위: A가 같다면 B를 비교해서 정렬.

5. 최종 코드 분석 (LeetCode 1356 정답)
Python

arr.sort(key=lambda x: (bin(x).count('1'), x))
* x: 리스트 arr에서 꺼낸 숫자.
* bin(x).count('1'): 첫 번째 정렬 기준 (1의 개수 적은 순).
* x: 두 번째 정렬 기준 (원래 숫자 작은 순).
💡 한 줄 요약:
"각 숫자를 (1의 개수, 자기 자신) 이라는 이름표로 바꿔서 줄을 세우되, 다 세우면 이름표는 버리고 원래 숫자만 남겨라!"
