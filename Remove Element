class Solution(object):
    def removeElement(self, nums, val):
        """
        :type nums: List[int] # 입력받는 정수 배열
        :type val: int       # 제거해야 할 타겟 값
        :rtype: int          # 타겟 값을 제외한 배열의 새로운 길이 k
        """
        
        # 1. k는 'val이 아닌 값'이 들어갈 인덱스이자, 
        #    최종적으로 반환할 배열의 길이를 나타냅니다.
        k = 0
        
        # 2. 배열의 전체 요소를 하나씩 확인합니다 (i는 현재 탐색 중인 위치).
        for i in range(len(nums)):
            
            # 3. 현재 요소(nums[i])가 지워야 할 값(val)과 다르다면?
            if nums[i] != val:
                
                # 4. 'val이 아닌 값'을 현재 k 위치에 덮어씌웁니다.
                #    (필요 없는 값은 자연스럽게 밀려나거나 덮어써짐)
                nums[k] = nums[i]
                
                # 5. 다음 'val이 아닌 값'을 저장하기 위해 k 위치를 한 칸 이동합니다.
                k += 1
        
        # 6. 최종적으로 저장된 유효한 요소의 개수(k)를 반환합니다.
        return k
